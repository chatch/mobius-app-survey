import {
  attribute,
  autoGeneratedHashKey,
  hashKey,
  table,
} from '@aws/dynamodb-data-mapper-annotations'

@table('results')
class Result {
  @autoGeneratedHashKey() id: string
  @attribute() surveyId: string
  @attribute() userId: string
  @attribute() json: string
}

export default Result
